# Enigma Engine Requirements
# Last Updated: December 2024
# Minimum Python version: 3.9+

# === CORE (Required) ===
python-dotenv>=0.19.0
flask>=2.0.0
flask-cors>=3.0.10
sqlalchemy>=1.4.0
numpy>=1.21.0

# === MODEL & TRAINING ===
# PyTorch - install manually for your platform:
# CPU: pip install torch torchvision torchaudio
# CUDA: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# Raspberry Pi: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
torch>=1.12; platform_system!="Darwin" or platform_machine=="arm64"

# Tokenizers (for BPE, optional - we have character-level built-in)
tokenizers>=0.13.0
transformers>=4.20.0

# === GUI (PyQt5) ===
# Note: On Raspberry Pi, use system package instead:
#   sudo apt install python3-pyqt5
PyQt5>=5.15.0

# === VOICE ===
pyttsx3>=2.90
SpeechRecognition>=3.8.0
sounddevice>=0.4.0
vosk>=0.3.0
# PyAudio - may need system dependencies:
#   Ubuntu/Debian: sudo apt install portaudio19-dev python3-pyaudio
#   macOS: brew install portaudio
#   Windows: pip will handle it
pyaudio>=0.2.11

# === VISION ===
pillow>=9.0.0
mss>=6.1.0
pyscreenshot>=3.0

# === DEVELOPMENT & TESTING (Optional) ===
# Uncomment for development work:
# pytest>=7.0.0
# pytest-cov>=3.0.0
# flake8>=5.0.0
# mypy>=0.990
# autopep8>=2.0.0

# === MONITORING (Optional) ===
# For resource monitoring:
psutil>=5.9.0

# === CAMERA (Optional) ===
# For webcam support:
# opencv-python>=4.5.0
# For Raspberry Pi camera:
# picamera2>=0.3.0

# === MOTION TRACKING (Optional) ===
# For user gesture mimicry:
# mediapipe>=0.10.0
# opencv-python>=4.5.0

# === 3D GENERATION (Optional) ===
# For local 3D model generation:
# shap-e>=0.0.1  # Shap-E for 3D generation
# point-e>=0.0.1  # Point-E alternative
# trimesh>=3.20.0  # For 3D mesh manipulation

# === GGUF MODELS (Optional) ===
# For loading llama.cpp compatible models:
# llama-cpp-python>=0.2.0

# === DOCUMENTS (Optional) ===
# Uncomment to enable document parsing:
# pypdf2>=2.0.0
# python-docx>=0.8.11
# ebooklib>=0.18

# === WEB (Optional) ===
# Uncomment to enable web scraping:
# requests>=2.28.0
# beautifulsoup4>=4.11.0

# === SAFETY & FILTERING ===
beautifulsoup4>=4.11.0    # For content extraction

# === VECTOR DATABASES (Optional) ===
# Uncomment to enable advanced memory features:
# faiss-cpu>=1.7.0  # For FAISS vector database (CPU version)
# faiss-gpu>=1.7.0  # For FAISS vector database (GPU version)
# pinecone-client>=2.2.0  # For Pinecone cloud vector database

# === SYSTEM MONITORING ===
psutil>=5.9.0             # For power mode CPU/memory monitoring

# === AI SERVICES (Optional, Cloud-based) ===
# Uncomment if using cloud AI services:
# openai>=1.0.0  # For DALL-E, GPT-4, embeddings
# replicate>=0.15.0  # For SDXL, video, music generation
# elevenlabs>=0.2.0  # For premium TTS

# === VIDEO & GIF EDITING (Optional) ===
# Uncomment to enable video editing and advanced GIF features:
# moviepy>=1.0.3  # For video editing, conversion to GIF, frame extraction

# === WEB DASHBOARD & MOBILE ===
flask-socketio>=5.0.0

# === HUGGINGFACE INTEGRATION ===
huggingface-hub>=0.20.0
# transformers already included above (>=4.20.0)

